spring:
  data:
    mongodb:
      port: ${mongo_port:27017}
      host: mongodb
  application:
    name: product-service


logging:
  level:
    org.springframework.cloud.consul: INFO
server:
  port: 8080